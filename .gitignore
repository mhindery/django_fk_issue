# Compiled source #
###################
*.com
*.class
*.dll
*.exe
*.o
*.so
*.pyc
__pycache__/
 
# Packages #
############
# it's better to unpack these files and commit the raw source
# git has its own built in compression methods
*.7z
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip
 
# Logs and databases #
######################
*.log
*.sql
*.sqlite
 
# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

#PyCharm
.idea

#libs
webapp/static
bin
lib/python2.7
python2.7
credentials.json
otainsight/saml2_config/certificates/*.crt
otainsight/saml2_config/certificates/*.pem
otainsight/saml2_config/certificates/*.key
bucket_static
google_drive_credentials.json
api-docs/slate
notebooks
.vscode
config/config_values.json

# Since this is frequently edited locally depending on what you're testing, it's annoyhing to exclude it every time from
# changes in git, so exclude this (the base version is present already).
scripts/run_local_tests.sh
translations/*.json

# terraform
.terraform
.terraform.lock.hcl

htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover
